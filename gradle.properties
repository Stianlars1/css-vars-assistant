# IntelliJ Platform already bundles the Kotlin standard library.
kotlin.stdlib.default.dependency=false

# Keep local and CI builds incremental.
org.gradle.configuration-cache=true
org.gradle.caching=true
